import { DiceNode } from "@/components/nodes/dice-node";
import { DiceSumNode } from "@/components/nodes/dice-sum-node";
import { HistogramNode } from "@/components/nodes/histogram-node";

export const NODE_TYPES = {
  dice: DiceNode,
  histogram: HistogramNode,
  diceSum: DiceSumNode,
};
