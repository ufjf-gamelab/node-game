import { DicePoolSumNode } from "./node";
import { DicePoolSumService } from "./service";

export const DicePoolSum = {
  component: DicePoolSumNode,
  service: DicePoolSumService,
  properties: null,
};
